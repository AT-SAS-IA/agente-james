version: '3.8'

services:
  api:
    build: .
    image: james-api:latest
    ports:
      - "8000:8000"
    # Las variables de entorno ENDPOINT, SUBSCRIPTION_KEY, DEPLOYMENT y API_VERSION
    # se configuran directamente en el portal de Azure Container Apps.
